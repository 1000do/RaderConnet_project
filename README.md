# Linked\_project

Đồ án Tốt nghiệp

Lần đầu set up docker thì mở cmd chạy lệnh này lên trước:
docker network create my-shared-network

lệnh chạy docker:
docker compose up -d --build
Lệnh xóa 1 lần build  chạy là 1 lần xóa:
docker compose down
lệnh giảm dung lịch hên xui:
docker system prune
